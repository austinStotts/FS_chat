import React from 'react';
import ReactDOM from 'react-dom';
import App from './app';

// index for all frontend react complonents
// renders root to html
ReactDOM.render(<App />, document.getElementById('root'));